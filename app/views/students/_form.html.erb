<%= form_for @student, :html => { :class => 'form-inline' } do |f| %>

<fieldset>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <ul>
      <% @student.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :registry, t("form.student.registry"), :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :registry, :class => 'text_field' %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :name, t("form.student.name"), :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :course_id, t("form.student.course") %>
    <%= f.collection_select :course_id, @courses, :id, :name, :prompt => t("course.prompt_field") %>
  </div>

  <div class="actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
  </div>
</fieldset>

<% end %>
