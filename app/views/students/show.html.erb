<p id="notice"><%= notice %></p>

<p>
  <b><%= t("student.name") %></b>
  <%= @student.name %>
</p>

<p>
  <b><%= t("student.course") %></b>
  <%= @student.course.name %>
</p>

<%= link_to 'Edit', edit_student_path(@student) %> |
<%= link_to 'Back', students_path %>
