<div class="row-fluid">
  <b><%= t("discipline.name") %>:</b>
  <%= @discipline.name %>
</div>

<div class="row-fluid">
  <b><%= t("discipline.teacher") %>:</b>
  <%= @discipline.teacher.try(:name) %>
</div>

<div class="row-fluid">
  <%= link_to t("form.teacher.register"), new_teacher_path %>
</div>

<div class="row-fluid">
  <%= link_to 'Edit', edit_discipline_path(@discipline), :class => 'btn btn-primary' %>
  <%= link_to 'Back', disciplines_path, :class => 'btn btn-primary' %>
</div>
